# Merge And State

Generated projects contain `.cyan_state.yaml`.

The state records template identity, answers, deterministic state, files, artifact pins, and integrity. Update uses this state to compare prior, current, and target output.
