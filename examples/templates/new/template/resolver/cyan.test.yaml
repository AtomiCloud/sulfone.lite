cases:
  - name: latest-wins
    resolverInputs:
      - path: tests/latest-wins/current
        origin:
          template: current
          layer: 1
      - path: tests/latest-wins/target
        origin:
          template: target
          layer: 2
    expected: tests/latest-wins/expected
    validations:
      - grep -q target shared.txt
