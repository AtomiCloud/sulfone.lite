# Probe Fixture: Built-ins

Generated by the probe-less built-ins fixture. It declares `tests`, `coverage`, `lint`, and `ci` but ships no probes of its own, so every feature is proven by the engine's built-in probe library; run `bash scripts/ci.sh`.
