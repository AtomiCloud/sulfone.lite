# Machine-generated by the CyanPrint probe engine — do not edit.
# Regenerate with the probe manifest generator; drift fails the gates.
contractVersion: 1
features:
  - template: cyanprint/probe-fixture-builtins
    name: ci
    probes:
      - name: builtin-ci-baseline-green
        description: The untouched repo passes its ci gate.
        kind: baseline
        origin:
          kind: built-in
      - name: builtin-ci-wiring-invokes-gates
        description: The CI entrypoint must statically reference at least one other
          quality gate.
        kind: baseline
        origin:
          kind: built-in
      - name: builtin-gate-failure-reddens-ci
        description: A sabotage a chained gate catches (a lint violation) must turn the
          ci gate red.
        kind: mutation
        origin:
          kind: built-in
  - template: cyanprint/probe-fixture-builtins
    name: coverage
    probes:
      - name: builtin-coverage-baseline-green
        description: The untouched repo passes its coverage gate.
        kind: baseline
        origin:
          kind: built-in
      - name: builtin-corrupting-coverage-ledger-reddens-gate
        description: Corrupting the committed coverage ledger must turn the coverage
          gate red.
        kind: mutation
        origin:
          kind: built-in
  - template: cyanprint/probe-fixture-builtins
    name: lint
    probes:
      - name: builtin-lint-baseline-green
        description: The untouched repo passes its lint gate.
        kind: baseline
        origin:
          kind: built-in
      - name: builtin-lint-error-reddens-gate
        description: Introducing a forbidden construct in src/ must turn the lint gate red.
        kind: mutation
        origin:
          kind: built-in
  - template: cyanprint/probe-fixture-builtins
    name: tests
    probes:
      - name: builtin-tests-baseline-green
        description: The untouched repo passes its tests gate.
        kind: baseline
        origin:
          kind: built-in
      - name: builtin-deleting-tests-reddens-gate
        description: Emptying the whole test set must turn the tests gate red (zero-test
          pass is the false green).
        kind: mutation
        origin:
          kind: built-in
