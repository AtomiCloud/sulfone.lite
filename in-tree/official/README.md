# Official In-Tree Artifacts

This folder contains CyanPrint-managed artifacts that are intended to be published to the production registry.

- `templates/new`: official `cyan/new` scaffold template.
- `processors/default`: official `cyan/default` processor.

Use these paths for official publish and seed checks. The `examples/` tree remains for broader fixtures and regression tests.
